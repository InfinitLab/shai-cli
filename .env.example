# Shai CLI Environment Configuration
#
# Copy this file to .env.development or .env.production and adjust as needed.
# Load with: source .env.development && shai <command>
# Or use dotenv gem for automatic loading.

# API base URL (default: https://shai.dev)
SHAI_API_URL=http://localhost:3000

# Configuration directory for storing credentials
# Use relative path for project-local storage (recommended for development)
# Use absolute path or ~ for global storage (production default: ~/.config/shai)
SHAI_CONFIG_DIR=.config/shai-dev

# Override authentication token (useful for CI/CD)
# SHAI_TOKEN=your-api-token-here

# Disable colored output
# NO_COLOR=1
